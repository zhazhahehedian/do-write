# ========== 通用 ==========
TZ=Asia/Shanghai

# 站点对外访问地址（用于 OAuth 回调地址拼装）
# 本地开发: http://localhost
# 生产环境: https://yourdomain.com（务必使用HTTPS）
APP_BASE_URL=http://localhost

# ========== 后端（Spring Boot） ==========
SPRING_PROFILES_ACTIVE=prod

# 数据库（MySQL）
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=do_write
MYSQL_USERNAME=root
MYSQL_PASSWORD=please-change-me

# Redis（建议生产环境务必设置强密码）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=please-change-me
REDIS_DATABASE=0

# ChromaDB
CHROMA_HOST=http://chroma
CHROMA_PORT=8000
CHROMA_COLLECTION=novel-memories
CHROMA_TENANT=default_tenant
CHROMA_DATABASE=default_database

# AI（占位：真实 Key 推荐在系统中配置或通过环境变量注入）
OPENAI_API_KEY=sk-placeholder-key
OPENAI_BASE_URL=https://api.openai.com
OLLAMA_BASE_URL=http://localhost:11434
DASHSCOPE_API_KEY=sk-placeholder-key
AI_ENCRYPTION_KEY=D0-Writ3-32b!t

# OAuth（第三方登录）
# Linux.do OAuth 2.0（需要申请 client_id 和 client_secret）
LINUXDO_CLIENT_ID=your-client-id
LINUXDO_CLIENT_SECRET=your-client-secret
# FishPi 使用 OpenID 2.0 协议，无需 client_id/secret

# ========== 前端（Next.js） ==========
# Next.js 重写代理到后端（不使用 Nginx 时也能正常工作）
BACKEND_URL=http://backend:8099

# 统一建议使用相对路径，走同源 /api
NEXT_PUBLIC_API_URL=/api
# 部分组件使用该变量拼接（不需要可留空）
NEXT_PUBLIC_API_BASE_URL=
